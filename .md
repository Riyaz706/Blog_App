npm create vite@latest

first clean up app.jsx,app.css and index.css


StateDemo info:
1. useState is a hook that is used to manage the state of a component.
2. useState returns an array of two values: the current state and a function to update the state.
3. The first value is the current state and the second value is a function to update the state.
when the rerendering happens?
1. when the state changes
2. when the props changes
3. when the parent component re-renders
4. when the component is mounted
5. when the context changes



1. update marks state by inserting elements at beginning,ending and in between with index
2. delete marks from end of the array

validation rules:
1. minlenght and maxlenght
2. min and max
3. required
4. pattern


lifitng state up:
1. when the state is lifted up, the state is moved to the parent component
2. when the state is lifted up, the state is passed to the child component as props
3. when the state is lifted up, the state is passed to the child component as props
4. when the state is lifted up, the state is passed to the child component as props

SideEffects
1. A component can render intially before displaying content
2. if the component is about to make a API request it should wait the intial rendering is completed
3. if both intial rendering and API request happeneed together it leeds to unexpected bugs in the application
4. useEffect is a hook that is used to manage the side effects of a component

=> no dependency array: runs on every render
useEffect(() => {side effect}, [])

=> empty dependency array: runs once on mount  
useEffect(() => {side effect}, [])

=> dependency array with values: runs when dependecied chnage 
useEffect(() => {side effect}, [value1, value2, ....])


---------------Routes:------------------
1.decide the root layout
2.install the react-router
3.create the components
4.configure the routing (connecting components to a path)